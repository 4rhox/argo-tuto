apiVersion: v1
kind: Pod
metadata:
  namespace: testName
  labels:
    run: nginx-bis
  name: nginx-bis
spec:
  containers:
  - image: nginx
    name: nginx
    resources: {}
  dnsPolicy: ClusterFirst
  restartPolicy: Always
